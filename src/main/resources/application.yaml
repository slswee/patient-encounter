ktor:
    application:
        modules:
            - com.sallyli.ApplicationKt.module
    deployment:
        port: 8080

api:
    keys: ${API_KEYS:""}

jwt:
    secret: ${JWT_SECRET:"dev-secret-change-in-production"}
